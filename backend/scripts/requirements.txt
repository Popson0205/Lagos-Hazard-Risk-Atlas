# Extra dependencies for build_dem_derived_layers.py only — NOT installed
# into the deployed backend (see backend/requirements.txt for that), since
# rasterio/scipy are heavy and only this one-off offline script needs them.
#
#   pip install -r backend/scripts/requirements.txt
#
pystac-client==0.8.3
rasterio==1.3.10
numpy==2.0.1
scipy==1.13.1
